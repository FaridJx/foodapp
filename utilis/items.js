export const getInfoItem = (item) => ({
    id: item.id,
    name: item.name,
    color: item.color,
    desc: item.desc,
    ingredients: item.ingredients,
    unit: item.unit,
    amount: item.amount,
    image: item.image,
    level: item.level,
    rating: item.rating,
    time: item.time,
    longDesc: item.longDesc,
  });